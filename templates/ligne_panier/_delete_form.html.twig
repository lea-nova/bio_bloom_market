<form
	method="post" action="{{ path('app_ligne_panier_delete', {'produitId': item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	{# <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_panier.id) }}"> #}
	<button class="border-2 rounded-md border-red-600 p-2 text-sm ">Supprimer</button>
</form>
