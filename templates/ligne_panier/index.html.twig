{% extends 'base.html.twig' %}

{% block title %}LignePanier index
{% endblock %}

{% block body %}
	<h1>LignePanier index</h1>

	<table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Quantite</th>
				<th>PrixTotal</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for ligne_panier in ligne_paniers %}
				<tr>
					<td>{{ ligne_panier.id }}</td>
					<td>{{ ligne_panier.quantite }}</td>
					<td>{{ ligne_panier.prixTotal }}</td>
					<td>{# <a href="{{ path('app_ligne_panier_show', {'id': ligne_panier.id}) }}">show</a> #}
						{# <a href="{{ path('app_ligne_panier_edit', {'id': ligne_panier.id}) }}">edit</a> #}
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="4">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	{# <a href="{{ path('app_ligne_panier_new') }}">Create new</a> #}
{% endblock %}
