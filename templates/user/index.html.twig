{% extends 'base.html.twig' %}

{% block title %}User index
{% endblock %}

{% block body %}
	<div class="container pt-5">
		<h1>User index</h1>
		<table class="table">
			<thead>
				<tr>
					{# <th>Id</th> #}
					<th>Email</th>
					{# {% if is_granted("ROLE_SUPER_ADMIN") %}
					
											<th>Roles</th>
										{% endif %} #}
					{# <th>Password</th> #}
					<th>Nom</th>
					<th>Prenom</th>
					{# <th>Telephone</th> #}
					<th>DateNaissance</th>
					<th>FideliteClient</th>
					<th>PrefAchat</th>
					{# {% if  is_granted('ROLE_SUPER_ADMIN') %} #}
					<th>actions</th>
					{# {% endif %} #}
				</tr>
			</thead>
		</tr>
	</thead>
	<tbody>
		{% for user in users %}
			<tr>
				{# <td>{{ user.id }}</td> #}
				<td>{{ user.email }}</td>
				{# {% if is_granted("ROLE_SUPER_ADMIN") %}
													<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
												{% endif %} #}
				{# <td>{{ user.password }}</td> #}
				<td>{{ user.nom }}</td>
				<td>{{ user.prenom }}</td>
				{# <td>{{ user.telephone }}</td> #}
				<td>{{ user.dateNaissance ? user.dateNaissance|date('Y-m-d') : '' }}</td>
				<td>{{ user.fideliteClient ? 'Yes' : 'No' }}</td>
				<td>{{ user.prefAchat }}</td>
				{# {% if  is_granted('ROLE_SUPER_ADMIN') %} #}

				<td>
					<a href="{{ path('app_user_show', {'id': user.id}) }}">show</a>
					{% if is_granted("ROLE_SUPER_ADMIN") %}
						<a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>
					{% endif %}
				</td>

				{# {% endif %} #}

			</tr>
		{% else %}
			<tr>
				<td colspan="11">no records found</td>
			</tr>
		{% endfor %}
	</tbody>
</tbody></table>{% if  is_granted('ROLE_SUPER_ADMIN') %}
<a href="{{ path('app_user_new') }}">Create new</a>{% endif %}</div>{% endblock %}
