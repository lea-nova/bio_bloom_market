<form method="post" action="{{ path('app_adresse_delete', {'ulid': adresse.ulid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse.id) }}">
	<button class="btn">Delete</button>
</form>
